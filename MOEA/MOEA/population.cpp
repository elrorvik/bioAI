#include"population.h"

Population::~Population() {

}

void Population::MST_Kruskal() {
	// Remove the two nodes with least edge weight (weight = "cost" = RGB distance) and add it to its own tree
	
}